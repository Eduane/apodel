import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      height:110,
        maxHeight: 110,
      },
    contentContainer:{
      height: 110,
alignItems:'center'

    }
 
});

export default styles;
